/**
 * Kernel plugin entry.
 *
 * The image bed plugin runs entirely in the front-end plugin context. This
 * lightweight entry keeps the template's kernel build target valid without
 * registering server-side APIs.
 */
console.log("OSS image bed kernel entry loaded.");
